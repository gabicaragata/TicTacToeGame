<Window x:Class="TicTacGame.WindowsApp.Views.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="About" Height="250" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Label Content="Student Name:" Grid.Row="0" HorizontalAlignment="Left" Margin="10"/>
        <TextBlock Name="txtStudentName" Grid.Row="0" Margin="10" HorizontalAlignment="Right"/>

        <Label Content="Email:" Grid.Row="1" HorizontalAlignment="Left" Margin="10"/>
        <TextBlock Name="txtEmail" Grid.Row="1" Margin="10" HorizontalAlignment="Right"/>

        <Label Content="Date:" Grid.Row="2" HorizontalAlignment="Left" Margin="10"/>
        <TextBlock Name="txtDate" Grid.Row="2" Margin="10" HorizontalAlignment="Right"/>

        <Label Content="Project Description:" Grid.Row="3" HorizontalAlignment="Left" Margin="10"/>
        <TextBlock Name="txtProjectDescription" Grid.Row="4" Margin="10" TextWrapping="Wrap"/>
    </Grid>
</Window>